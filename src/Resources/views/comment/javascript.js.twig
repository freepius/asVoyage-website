$(document).ready(function () {

{# Ask confirmation before delete a comment #}
{% if is_granted('ROLE_ADMIN') %}

    $('.comment-delete').click(function () {
        return confirm("{{ 'comment.deleting.confirm'|trans }}");
    });

{% endif %}

});
