<article itemscope itemtype="http://schema.org/BlogPosting" class="summary">

    <header class="row">
        <h1 itemprop="name" class="pull-left">{{ title }}</h1>

        <time itemprop="datePublished" datetime="{{ pubdatetime }}" class="pull-right">
            {{ pubdatetime | localizeddate('full') | capitalize }}
        </time>
    </header>


    <div class="row" itemprop="articleBody">
        {{ (summary ?: text) | raw }}

        <big class="pull-right">
            <a itemprop="url" href="/blog/{{- slug -}}/read">» {{ 'Read more'|trans }}</a>
        </big>
    </div>


    <footer class="row">
        <div class="span8">
            <b>{{ 'blog.field.tags'|trans }} :</b>

            {% if tags is empty %}
                {{ 'none'|trans }}
            {% else %}
                <span itemprop="keywords">{{ tags | join(', ') }}</span>
            {% endif %}
        </div>

        <!-- Actions on article -->
        <div class="pull-right">
            <!-- Update -->
            <a href="/blog/{{- slug -}}/update" title="{{ 'Update'|trans }}">
                <i class="icon-pencil"></i></a>

            <!-- Delete -->
            <a href="/blog/{{- slug -}}/delete" title="{{ 'Delete'|trans }}">
                <i class="icon-trash"></i></a>
        </div>
    </footer>

</article>
